{% extends 'adminbase.html.twig' %}

{% block title %}CurrencyType{% endblock %}

{% block body %}
<div class="content">
    <main>
    <h1>CurrencyType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ currency_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ currency_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('currency_type_index') }}">back to list</a>

    <a href="{{ path('currency_type_edit', {'id': currency_type.id}) }}">edit</a>

    {{ include('admin/currency_type/_delete_form.html.twig') }}
    </main>
</div>
{% endblock %}
