{% extends 'adminbase.html.twig' %}

{% block title %}Expansion{% endblock %}

{% block body %}
<div class="content">
    <main>
    <h1>Expansion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ expansion.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ expansion.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('expansion_index') }}">back to list</a>

    <a href="{{ path('expansion_edit', {'id': expansion.id}) }}">edit</a>

    {{ include('admin/expansion/_delete_form.html.twig') }}
    </main>
</div>
{% endblock %}
